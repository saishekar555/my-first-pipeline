trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: echo "✅ Pipeline working"
  displayName: 'Test Pipeline'

- script: |
    echo "Listing files"
    ls -la
  displayName: 'Check Files'